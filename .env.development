# Development only: the browser uploads directly to the local FastAPI server,
# because the Next.js dev proxy cannot forward large request bodies.
# In production this variable is unset and requests stay same-origin.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
